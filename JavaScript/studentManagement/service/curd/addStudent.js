// Add Student

// Approach:
// 1. Receive the new student.
// 2. Add the student to the array.
// 3. Return the updated students.
export function addStudentFunctionality(student, newStudent) {
  student.push(newStudent);
  return student;
}
