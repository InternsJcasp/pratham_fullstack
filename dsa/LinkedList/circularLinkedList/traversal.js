// function traversal(head) {
//   if (head === null) {
//     return;
//   }
//   let current = head;

//  because we want to process the first node at least once
//   do {
//     console.log(current.data);
//     current = current.next;
//   } while (current !== head);
// }
